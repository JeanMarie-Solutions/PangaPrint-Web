services:
  - type: web
    name: pangaprint
    runtime: python3
    buildCommand: pip install -r requirements-server.txt && python manage.py collectstatic --noinput
    startCommand: python manage.py migrate && gunicorn pangaprint.wsgi:application
    envVars:
      - key: DEBUG
        value: False
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        fromService:
          type: web
          name: pangaprint
          property: host
      - key: DATABASE_URL
        fromDatabase:
          name: pangaprint-db
          property: connectionString

  - type: pserv
    name: pangaprint-db
    runtime: postgresql
    envVarGroups:
      - key: DATABASE_URL
        fromDatabase:
          name: pangaprint-db
          property: connectionString